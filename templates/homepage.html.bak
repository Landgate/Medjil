{% extends 'base_generic.html' %}
{% load static %}
 
{% block content %}

<article class="post-plus-sidebar" style="position:relative">
    <div class="left-sidebar px-3">
				<div class="mt-3">
			  		<ul class="tab-nav"><b>Medjil Guides</b>
					  		<li class="pxl-2"><a href="{% url 'edm_calibration:step_by_step_guide' %}">Range calibration</a></li>
					      <li class="pxl-2"><a href="{% url 'edm_calibration:step_by_step_guide' %}">Staff Calibration</a></li>
					      <li class="pxl-2"><a href="{% url 'edm_calibration:step_by_step_guide' %}">EDM Calibration</a></li>
					      <li class="pxl-2"><a href="{% url 'edm_calibration:step_by_step_guide' %}">EDMI Calibration</a></li>
			      </ul>
				</div>
    </div>
    <div class="main-post" style="background-color: white">
        {% if messages %}
            <ul class="alert messages">
                {% for message in messages %}
                <li {% if message.tags %} class="{{ message.tags }}"{% endif %}>{{ message }}</li>
                {% endfor %}
            </ul>
        {% endif %}
        <br>
        <div lass="mt-3">
            {% if not user.is_authenticated %}
            <p class="text-center"><a href="{% url 'accounts:login' %}" class="hover:a">< Login </a> or<a href="{% url 'accounts:signup' %}" class="hover:a"> signup</a> to manage and calibrate your survey instruments! ></p>
            {% endif %}
        </div>
        <div>
            <div class="mt-3 px-6">
                <div>
                    <h2  class="text-center">Welcome to Medjil the Survey Instrument Calibration Portal</h2>
                    <img src="{% static 'WelcomeOnHmepage.jpg' %}">
                    <div class="mt-3">                  
                        <p>Electronic Distance Measurement Instruments (EDMI) or barcoded staves are commonly used survey instruments for measuring length (distance) and hight differences. Regular calibration of these instruments is recommended and/or required
                        	 to ensure the measured quantities by these instruments are legally traceable back to the national and international standards. It is recommended that as a matter of practice, survey instruments should be calibrated every six months or after service or repaire. 
                        	  A series of measurements on a baseline can also be used to assess insrumnent precision against the manufacturer’s specfications. Midjil enables users to calibrate their instruments using EDM or Range calibration baselines.</p>                     
                        <br>
                        <p>For a specyfic requirements or technical information refer to your State Regulations and Guidelines or contact your local ‘Verifying Authority’ of length.</p>
                    </div>
                </div>
            </div>
        </div>
    </div>
</article>

<script>
    let slideIndex = 1;
    showSlides(slideIndex);

    function plusSlides(n) {
        showSlides(slideIndex += n);
    }

    function currentSlide(n) {
        showSlides(slideIndex = n);
    }

    function showSlides(n) {
        let i;
        let slides = document.getElementsByClassName("homeSlides");
        // let dots = document.getElementsByClassName("dot");
        if (n > slides.length) {slideIndex = 1}    
        if (n < 1) {slideIndex = slides.length}
        for (i = 0; i < slides.length; i++) {
            slides[i].style.display = "none";  
        }
        // for (i = 0; i < dots.length; i++) {
        //     dots[i].className = dots[i].className.replace(" active", "");
        // }
        slides[slideIndex-1].style.display = "block";  
    // dots[slideIndex-1].className += " active";
    }

    const edmClick = document.getElementById('edm-click')

    edmClick.addEventListener('click', () => {
        edmClick.style.display = "block";
    })
</script>

 {% endblock content %}